Build a polished one-page React demo called “Bimo Fleet.”

Show the deployed workflow as three connected stages: Engineering, QA, and
Testing. Make the QA and Testing failure path back to Engineering immediately
understandable without requiring a legend. Show a completed sample run with
one QA rejection followed by a successful second pass, plus compact facts for
five agent attempts, three ephemeral roles, and one published artifact.

Use an intentional dark terminal/editor aesthetic with warm off-white text,
one vivid signal color, generous spacing, and crisp typography. It should feel
like a serious developer tool, not a generic analytics dashboard. Add a small
“Replay workflow” control that visibly steps through the sample run without
network access. Replay must complete without an out-of-range step or uncaught
browser error, and it must work again after completion. Respect reduced-motion
preferences.

The page must be responsive and keyboard accessible. Use semantic HTML,
visible focus states, and sufficient contrast. Do not use remote assets,
analytics, a backend, credentials, or environment-specific URLs.
