Build one small React task-list app as a bounded parallel engineering pod.

The result is one coherent app for a single user. It should support adding a
non-empty task, marking a task complete, removing a task, and showing a useful
empty state. Use accessible labels and buttons, keep the layout usable at
mobile and desktop widths, and add a repeatable check for the core
interactions.

Coordinate the work as one bounded change: keep application code, starter
assets, and tests coherent; reconcile overlapping edits before verification;
and finish with the app checks passing. Do not add accounts, network services,
or deployment work. The requested outcome is the app and its evidence, not a
runtime or release operation.
